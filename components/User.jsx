export default function User({ variant, className = "" }) {
    const variantMap = {
        admin: "bg-primary",
        student: "bg-secondary text-dark",
        teacher: "bg-danger",
        parent: "bg-danger"
      };
    return (
        <User
            
        />
    )
}